language: generic
sudo: required
services: 
  - docker

before_install:
  - docker build -t bertiewooster/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true bertiewooster/docker-react npm run test